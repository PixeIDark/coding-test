// 분수를 순환소수로 전환. 반복되는 부분은 괄호로 묶어서 문자열로 반환, 분모 !== 0
// TODO: 벽
var fractionToDecimal = function (numerator, denominator) {
    const num = numerator / denominator

    // 0.2222123123123123123
    // 순환이아닌 불규칙이 어디서 끝나는지만 알아도 괜찮
};